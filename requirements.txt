# This file is generated from pyproject.toml (single source of truth).
# To install runtime dependencies in editable mode:
#   pip install -e .
# For development extras:
#   pip install -e .[dev,docs]
# For optional feature groups:
#   pip install -e .[viz,opt,ml,chem]

# Use the package metadata for dependency resolution
-e .
